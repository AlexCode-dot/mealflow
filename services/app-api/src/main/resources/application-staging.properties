# Mongo for staging (inject via SPRING_MONGODB_URI in CI/CD)
spring.mongodb.uri=${SPRING_MONGODB_URI:mongodb://staging-mongo-host:27017/app-db}

# Adjust log levels as needed for staging
logging.level.org.springframework.security=INFO
